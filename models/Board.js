module.exports = (sequelize, DataTypes) => {
  const Board = sequelize.defind('Board', {
    idx: {
      field: 'idx',
      type: DataTypes.INTEGER,
      allowNull: false,
      autoIncrement: true,
      primaryKey: true,
    }
  })fhnfffbfb
}
